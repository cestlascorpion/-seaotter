syntax = "proto2";

package scorpion;

message CallInfo {
  required uint32 ip = 1;
  required uint32 port = 2;
  required uint32 total = 3;
  required uint32 success = 4;
}

message Reports {
  repeated CallInfo info = 1;
}